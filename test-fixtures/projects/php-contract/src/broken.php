<?php

// Deliberately broken PHP so the contract snapshot always sees a diagnostics
// record and can pin its key shape (file_path, rel_file_path, parse_error{...}).
function busted( {
    return @@@ ;
